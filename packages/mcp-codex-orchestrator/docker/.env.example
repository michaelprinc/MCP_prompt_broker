# MCP Codex Orchestrator - Environment Configuration
# Copy this file to .env and fill in the values

# =============================================================================
# AUTHENTICATION (Choose ONE method)
# =============================================================================

# METHOD 1: ChatGPT Plus/Pro/Team Authentication (RECOMMENDED)
# -----------------------------------------------------------------------------
# Use your ChatGPT subscription. Run 'codex login' locally first, then
# the auth.json from ~/.codex will be automatically mounted into the container.
# No API key needed!

# Path to your .codex directory containing auth.json (default: ~/.codex)
# CODEX_AUTH_PATH=~/.codex

# METHOD 2: OpenAI API Key (pay-as-you-go)
# -----------------------------------------------------------------------------
# Only needed if you prefer usage-based billing instead of ChatGPT subscription
# OPENAI_API_KEY=sk-your-api-key-here

# =============================================================================
# GEMINI OAUTH AUTHENTICATION
# =============================================================================

# Path to your .gemini directory (default: ~/.gemini)
# GEMINI_AUTH_PATH=~/.gemini

# Optional: Google Cloud project for Code Assist org licenses
# GOOGLE_CLOUD_PROJECT=your-project-id

# =============================================================================
# OPTIONAL - Paths
# =============================================================================

# Path to workspace directory (default: ../workspace)
# WORKSPACE_PATH=../workspace

# Path to runs directory for logs and results (default: ../runs)
# RUNS_PATH=../runs

# =============================================================================
# OPTIONAL - Docker
# =============================================================================

# Codex runner Docker image name (default: codex-runner:latest)
# CODEX_IMAGE=codex-runner:latest

# Gemini runner Docker image name (default: gemini-runner:latest)
# GEMINI_IMAGE=gemini-runner:latest

# Run ID for container naming (set by orchestrator)
# RUN_ID=

# =============================================================================
# OPTIONAL - Codex Settings
# =============================================================================

# Enable quiet mode for Codex (default: 1)
# CODEX_QUIET_MODE=1

# Enable full-auto mode by default (default: false)
# CODEX_FULL_AUTO=false

# =============================================================================
# OPTIONAL - Orchestrator Settings
# =============================================================================

# Default timeout in seconds (default: 300)
# DEFAULT_TIMEOUT=300

# Log level: DEBUG, INFO, WARNING, ERROR (default: INFO)
# LOG_LEVEL=INFO

# MCP server port for HTTP transport (default: 3000)
# MCP_PORT=3000
